import React from 'react';
import './AddBlockForm.css';

function AddBlockForm() {
  // Form to add new blocks
  return <div>Form to add new blocks will be here.</div>;
}

export default AddBlockForm;
